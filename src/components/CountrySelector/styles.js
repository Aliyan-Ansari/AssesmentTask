import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    pickerWrapper: {
      marginVertical: 16,
      borderWidth: 2,
      borderRadius: 8,
    },
    picker: {
      borderRadius: 8,
    },
  });